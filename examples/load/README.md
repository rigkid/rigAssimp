# rigAssimp — load

Hero for [rigAssimp](../..). Loads `data/prism.stl` and `data/cube.obj` through Assimp into `CMesh`, presents with rigRender3D.

```bash
cmake -S . -B build
cmake --build build --target load
```
